# AI-Powered-Script-to-Reel-Generator-for-Social-Media-Influencers

Configure your `.env` with `LLM_MODEL` to a Groq model you have access to. The backend defaults to `llama-3.3-70b-versatile` and will fall back to that if the chosen model is unavailable.
